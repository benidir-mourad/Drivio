services:
  postgres:
    image: postgres:16-alpine
    container_name: drivio_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: drivio
      POSTGRES_USER: drivio
      POSTGRES_PASSWORD: secret
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  adminer:
    image: adminer:latest
    container_name: drivio_adminer
    restart: unless-stopped
    ports:
      - "8080:8080"
    depends_on:
      - postgres

volumes:
  postgres_data:
